{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run compile:styles",
    "predev": "bash ./bin/copy-assets.sh",
    "compile:styles": "stylus --watch ./src/css/*-page.styl --out ./static/css/."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "stylus": "^0.59.0"
  }
}
